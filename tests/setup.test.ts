/**
 * Placeholder test to satisfy vitest "exits 0" requirement.
 * Real tests are added in subsequent plans (02-02 through 02-05).
 */
import { describe, it } from 'vitest'

describe('scaffold', () => {
  it('test suite is configured', () => {
    // vitest exits 1 if no test files exist; this placeholder ensures exit 0
  })
})
